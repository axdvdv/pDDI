# Owner/oracle key — must equal the contract owner (the deployer). 0x-prefixed.
PRIVATE_KEY=0x...
# Deployed ProtocolBet address.
CONTRACT_ADDRESS=0x...
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
INTERVAL_MS=30000
